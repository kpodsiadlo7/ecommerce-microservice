server:
  port: 8888

spring:
  application:
    name: gateway
  main:
    web-application-type: reactive
#Eureka client
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    registerWithEureka: true
    fetchRegistry: true